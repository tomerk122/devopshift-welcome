pipeline {
    agent any
    stages {
        stage('Stage Name') {
            steps {
                // Commands or scripts to executefff
            }
        }
    }
    post {
        always {
            // Actions to take regardless of pipeline success or failure
        }
    }
}
