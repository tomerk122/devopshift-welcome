pipeline {
    agent any
    stages {
        stage('Stage 1 test') {
            steps {
                script {
                    // השתמש בגרשים כפולים כדי לעטוף את המילה שכוללת רווחים
                    sh  'echo "Fuck this Jdsdendfdffddsfkins" > fuckths.txt'
                }
                archiveArtifacts artifacts: 'fuckths.txt', allowEmptyArchive: true
            }
        }
    }
    post {
        always {
            echo "Done"
        }
    }
}
