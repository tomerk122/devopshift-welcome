pipeline {
    agent any
    stages {
        stage('Stage 1 test') {
            steps {
                echo "Fuck this Jenkins" > fuckths.txt
                 archiveArtifacts artifacts: 'path/to/file_or_pattern', allowEmptyArchive: true
            }
        }
    }
    post {
        always {
            echo "Done"
        }
    }
}
